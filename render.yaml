services:
  - type: web
    name: strength-intelligence-backend
    env: docker
    buildContext: backend
    dockerfilePath: Dockerfile
    branch: main
    plan: starter
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 5000
