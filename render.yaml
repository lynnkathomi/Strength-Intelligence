services:
  - type: web
    name: strength-intelligence-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    branch: main
    plan: starter
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 5000
